_schema-version: 3.3.0
ID: LeagueOfLegends
description: A simple CAP project.
version: 1.0.0
modules:
- name: LeagueOfLegends-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: LeagueOfLegends-auth
  - name: LeagueOfLegends-destination
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    instances: 1
  build-parameters:
    builder: npm-ci
- name: LeagueOfLegends-app-deployer
  type: com.sap.application.content
  path: gen
  requires:
  - name: LeagueOfLegends-auth
  - name: srv-api
  - name: LeagueOfLegends-html5-repo-host
    parameters:
      config:
        HTML5Runtime_enabled: true
      content-target: true
  build-parameters:
    build-result: app/
    requires:
    - artifacts:
      - historicololleagueoflegends.zip
      name: historicololleagueoflegends
      target-path: app/
- name: historicololleagueoflegends
  type: html5
  path: app/leagueoflegends
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: LeagueOfLegends-auth
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      tenant-mode: dedicated
      xsappname: LeagueOfLegends-${org}-${space}
    path: ./xs-security.json
    service: xsuaa
    service-plan: application
- name: LeagueOfLegends-destination
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
    service: destination
    service-plan: lite
- name: LeagueOfLegends-html5-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm ci
    - npx cds build --production
